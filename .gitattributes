# Normalize all text files to LF in the repo (Linux CI, consistent diffs).
* text=auto eol=lf

# Binaries / lockfiles that must not be munged
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
