# Builds the GrowthOS web image from the monorepo root and pushes it to Artifact Registry.
# Usage: gcloud builds submit --config deploy/cloudbuild.web.yaml --substitutions _IMAGE=<image> .
steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-f', 'apps/web/Dockerfile', '-t', '${_IMAGE}', '.']
images:
  - '${_IMAGE}'
options:
  machineType: 'E2_HIGHCPU_8'
timeout: 1800s
