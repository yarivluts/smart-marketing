/** One industry's share of all landed firmographic profiles in the window a caller scoped `records` to — the "#" (count) composition axis. */
export interface FirmographicIndustryCount {
  readonly industry: string;
  readonly count: number;
}
