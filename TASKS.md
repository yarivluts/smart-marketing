# TASKS — GrowthOS backlog (mirror of Jira project KAN)

This mirrors the Jira **KAN** (GrowthOS) backlog. Epics are KAN-1..KAN-16; the buildable stories are
KAN-17..KAN-78, listed below. Keep this file in sync with Jira as work progresses.

**Status legend:** `todo` · `in-progress` · `done` · `needs-human` (a human must act — accounts,
billing, secrets, external approvals) · `blocked-by` (waiting on another story; see Notes).

**How a scheduled run uses this file:** read [PROGRESS.md](./PROGRESS.md) + this file, pick the next
`todo` in sprint order (lowest sprint first), skipping `needs-human` and `blocked-by` items whose
blocker is unfinished. See [CLAUDE.md](./CLAUDE.md) for the full working rules.

## Human-action queue (do these out of band)

- **KAN-43** — apply for Google Ads developer token + Meta app / Marketing API review. LONG LEAD:
  submit in week 1; it gates KAN-50 and KAN-51.
- **KAN-18** — create GCP/Firebase projects (dev/staging/prod), enable billing, provision secrets.
  Gates most infra-dependent stories (ingest pipeline, warehouse, auth against a real project).
- General: any cloud account creation, API secrets, and billing setup.

## Stories

| KAN | Story | Phase | Sprint | Status | Notes |
| --- | ----- | ----- | ------ | ------ | ----- |
| KAN-17 | E0.1 Monorepo scaffold: apps/web (Next.js+TS+Tailwind+shadcn), apps/api (NestJS), packages/shared, packages/firebase-orm-models | 0 | 1 | done | Delivered by this bootstrap (E0.1 monorepo scaffold). |
| KAN-18 | E0.2 GCP projects (dev/staging/prod) via Terraform: Firestore, BigQuery, Pub/Sub, Secret Manager, Cloud Run, Redis | 0 | 1 | in-progress | GCP infra exists (human-confirmed 2026-07-20, built interactively): project `growthos-g2w84`, Cloud Run (web-dev/web-prod/api-dev/api-prod, me-west1), Firestore Native + CG index exemptions, Artifact Registry, Secret Manager (google-ads-developer-token, meta-user-access-token), Cloud Build deploy configs in deploy/, 500 ILS budget. `infra/terraform/` Terraform-izes the resources above plus `bigquery.tf` (`growthos_raw`/`growthos_core` datasets + `raw_records` table) — `terraform fmt`/`validate` green (CI-checked), but **not applied**: every resource needs `terraform import`/`apply` against the real project first (see `infra/terraform/README.md`) since nothing here was created by Terraform. IAM invoker bindings and Firestore collection-group index exemptions are deliberately unmodeled (need real-project credentials to read back correctly, not guessed). Phase 2 (buildable ahead of the terraform apply, done): a real `BigQueryWarehouseQueryExecutor` (`packages/firebase-orm-models/src/warehouse/bigquery-query-executor.ts`, `@google-cloud/bigquery`-backed, fake-client-tested, normalizes BigQuery's typed DATE/TIMESTAMP/bigint row values) now exists behind an env-driven `defaultWarehouseQueryExecutor` factory (`GOOGLE_CLOUD_PROJECT`/`GCLOUD_PROJECT` + `GROWTHOS_BIGQUERY_CORE_DATASET`/`GROWTHOS_BIGQUERY_LOCATION`) — inert until the dataset/tables actually exist, but wired end to end with zero caller changes needed once they do. Phase 3 (done): the `RawRecordModel`→BigQuery export job — a real `BigQueryRawRecordSink` (`packages/firebase-orm-models/src/warehouse/bigquery-raw-sink.ts`, fake-client-tested) streams every landed raw record into `growthos_raw.raw_records` alongside the existing Firestore write, via a new `DualWarehouseSink` (`pipeline/sink.ts`) wired in behind an env-driven `resolveWarehouseSinkFromEnv()` (`GROWTHOS_BIGQUERY_RAW_DATASET`, same posture as phase 2) — inert until the dataset/table exist. Phase 4 (done): `dbt-transform`'s `bigquery` profile target + DuckDB→BigQuery SQL dialect port — a real `prod` (type `bigquery`) output in `dbt/profiles.yml`, and every model/macro rewritten to compile identically against either adapter (`dbt/macros/cross_database.sql`: portable JSON-field extraction, tolerant casts, date diff/trunc, a correlated integer-range join, and a shared string-safe surrogate-key convention). `stg_raw_records` switches at compile time between the DuckDB seed and a new `growthos_raw.raw_records` dbt **source** pointed at the real terraform-declared table. Verified via a real `dbt build --target dev` (118/118, fixture-identical results) and `dbt parse --target prod` (real `dbt-bigquery` adapter, clean manifest/macro-dispatch — see `requirements-bigquery.txt`); **not** run against a live BigQuery project (KAN-18 still `needs-human`) — see `packages/dbt-transform/README.md`'s own verification-status note. `schema_identity_fields` (bridge_identity's seed) deliberately NOT ported — documented follow-up. This closes out every self-contained, no-live-infra-required KAN-18 slice identified so far. **Update 2026-08-19:** the real infra now exists — Yariv ran the `bq mk` provisioning commands himself (an automated run's own attempt to create the datasets/table directly was blocked by this environment's safety controls around creating live cloud infra); `growthos_raw`/`growthos_core` datasets and the partitioned/clustered `raw_records` table are confirmed live (`bq ls`/`bq show`, independently by two sessions). The compute service account already carries `roles/editor` project-wide — no separate BigQuery IAM grant was needed. `GOOGLE_CLOUD_PROJECT`+`GROWTHOS_BIGQUERY_RAW_DATASET` are now set on `web-dev`/`api-dev` (both redeployed clean) — new ingest events on dev dual-write into real `growthos_raw.raw_records` alongside Firestore; `web-prod`/`api-prod` and the query-executor's own env vars (`GROWTHOS_BIGQUERY_CORE_DATASET`/`GROWTHOS_BIGQUERY_LOCATION`) deliberately NOT set yet — the `growthos_core` tables (`entities`/`events`/`fact_engagement_daily`/etc.) don't exist in BigQuery until `dbt build --target prod` actually runs against it, and flipping the query executor on before that would turn every board tile's clean "warehouse not configured" state into a hard "table not found" crash (`board.service.ts`'s catch block only handles `WarehouseNotConfiguredError`/`ProjectQueryQuotaExceededError`/compiler errors — anything else rethrows). `dbt debug --target prod` connects cleanly (real oauth ADC, all checks passed, verified locally) but `dbt build --target prod` (which issues real `CREATE TABLE` DDL) hit the identical live-infra-creation block as the dataset provisioning — confirming the block is about creating/writing real cloud resources specifically, not a one-off. **Update 2026-08-19 (later): the warehouse is LIVE end to end on dev AND prod.** With Yariv approving commands per-command (the working pattern for BigQuery-writing operations from an interactive session), `dbt build --target prod` ran for real: first run surfaced 3 gaps `dbt parse` couldn't catch (schema-name concatenation scattering tables into `growthos_core_core`/`_staging`/`_seed`; DuckDB-typed seeds failing to load; DuckDB-only fixture tests) — all fixed in PR #102 (with a new `generate_schema_name` override, seeds/tests gated to duckdb, + a typed `WarehouseQueryFailedError` so a metric naming an unbuilt mart table degrades that one tile instead of crashing the whole board page, which it otherwise did). Second run: 57/57 PASS, all 6 core tables + 4 staging views confirmed in `growthos_core`; stray side-datasets deleted. Dual-write verified with a real event (ingest 202 → `bq query` shows the row → dbt folds it into `events` + `fact_engagement_daily`, dau=1). **First real board tile rendered** (session B, screenshot-verified: `dau_mau_ratio` big-number tile showing the real datapoint), all 3 pre-existing boards degrade per-tile cleanly. All four BigQuery env vars live on all 4 Cloud Run services (fresh images from main — an env-var-only update redeploys the same stale image, which bit once). Remaining for done: scheduled dbt orchestration against real BigQuery (KAN-38's executor still shells out to the DuckDB dev target; refresh is currently a manual per-command-approved `dbt build --target prod`), per-environment dataset split (dev/prod currently share the one `growthos_raw`/`growthos_core` pair, distinguished only by `environment_id` in raw), `estimated_cost_usd` from BigQuery job stats, terraform import/apply reconciliation, Pub/Sub, Redis (parked: human cost decision), staging env. |
| KAN-19 | E0.3 CI/CD (GitHub Actions): lint, typecheck, tests, preview deploy per PR, auto-deploy staging | 0 | 1 | in-progress | CI (lint/typecheck/test/build) green via .github/workflows/ci.yml; preview + staging deploy still needs infra. |
| KAN-20 | E0.4 Observability baseline: OpenTelemetry, structured logs, Sentry, uptime checks | 0 | 1 | done | Reconciled the three unmerged duplicate PRs (#2, #3, #5 — stale since 2026-07-04, ~50 runs deferred to human judgment). Synthesized from PR #3 (`@sentry/nestjs`/`@sentry/nextjs`, api+web+docs, AsyncLocalStorage trace propagation — avoids a second hand-rolled OpenTelemetry SDK) with PR #5's real bug fixes ported in (LOG_LEVEL="" boot crash, secret redaction, `isEnvironment()` reuse) plus a new fix found during the rebase itself (trace-context's `node:async_hooks`/`node:crypto` imports were breaking apps/web's client bundle via the root barrel — moved to a `@growthos/shared/observability` subpath, server-only) and an x-request-id-header-trust hardening (only reused when it already matches a trace-id shape). PR #2 and PR #5 closed as superseded. |
| KAN-21 | E1.1 Firebase Auth integration (email + Google SSO) + session handling in Next.js | 0 | 1 | done | Delivered in apps/web: email/password + Google SSO via the Firebase client SDK (`lib/auth/auth-context.tsx`), an httpOnly session cookie minted server-side via firebase-admin (`app/api/auth/session/route.ts`), fail-closed middleware gating (`middleware.ts`, cookie-presence only — Edge runtime) plus real cryptographic verification per protected/auth route (`lib/auth/get-server-session.ts`) for `/dashboard`, `/login`, `/signup`. Wires the authenticated principal into the KAN-24 `PermissionProvider` (bindings still empty pending KAN-22/26 role-binding lookup). Tested against a real local Firebase Auth emulator: vitest unit/integration coverage + a Playwright E2E suite driving real sign-up/sign-in/sign-out through a live Next.js server and browser, including a regression test for a lockout bug the review process caught (see PROGRESS.md). |
| KAN-22 | E1.2 firebase-orm models: global User, Organization, Membership (user-org many-to-many), Project, Environment, RoleBinding, ServiceAccount | 0 | 1 | done | Models were already scaffolded (KAN-79 bootstrap); this run added the missing "CRUD via Firestore-emulator tests" AC: a real local Firestore emulator wired into `pnpm test` (`firebase.json` + `firebase-tools`), full CRUD coverage per model, one user active in 2 orgs with different roles, and `removeMembershipCascade()` (cascade-deletes a user's role bindings when their membership is removed). |
| KAN-23 | E1.3 Policy engine: permission catalog, role bundles, inheritance org->project->env, deny-by-default | 0 | 1 | done | Delivered in `packages/shared/src/policy` (KAN-79 run 2). 138-case table-driven test matrix (role x permission x level). The "unauthorized API call -> 403" half of the AC has no route to test against yet — apps/api only has a health check; wiring the engine into route guards is KAN-24. |
| KAN-24 | E1.4 authz middleware/decorator for all API routes + client-side permission gate hooks | 0 | 2 | done | Delivered: `PermissionGuard` (global `APP_GUARD`) + `@RequirePermission`/`@Public` decorators in apps/api, wired to the KAN-23 policy engine; a custom eslint rule fails the build on any route missing an explicit annotation; `usePermission`/`PermissionProvider` mirrors the same check in apps/web. `request.principal`/`request.bindings` are populated once apps/api verifies the KAN-21 session server-to-server and KAN-22/26 (role-binding lookup) land — until then every non-public API route denies. KAN-21 (done) only covers the Next.js side (apps/web); wiring the Firebase session into apps/api's own requests is separate follow-on work. |
| KAN-25 | E1.5 UI: org-scoped sessions, org switcher (memberships only), project switcher, env badge, create/invite/join flows | 0 | 1 | done | Delivered: `packages/firebase-orm-models`'s `organization.service.ts`/`invite.service.ts`/`user.service.ts` (org/project/environment creation, cross-org membership listing via a collection-group query, email-based invite/accept with an identity-based mismatch check and an email-verification account-takeover gate) and `apps/web`'s org switcher, project switcher, env badge, member list, invite/join flow pages + API routes, wiring a real role-binding lookup into the KAN-24 `PermissionProvider` (closing the `bindings: []` placeholder from KAN-21/24). Two self-review rounds during implementation plus an independent review round after merge-readiness (see PROGRESS.md) fixed a real over-scoped-invite privilege-escalation bug, an account-takeover identity-resolution bug, and an invite-accept check-ordering info leak; added missing test coverage for two untested components and the whole project-switcher/env-badge feature area (e2e), and a regression test pinning `INVITABLE_ROLES` against `ROLE_SCOPE_LEVELS`. |
| KAN-26 | E1.6 Hard-isolation & non-enumeration layer: 404-not-403, binding-filtered lists, scoped caches/search/notifications, per-project datasets | 0 | 2 | done | Delivered for the surfaces that exist today (orgs/projects/members/invites in apps/web — caches/search/notifications/per-project datasets don't exist yet per docs/plan/08-generic-platform.md §5.6, so those bullets are forward-looking for later epics). `requireOrgPermission` now returns 404 (not 403) when the caller has no active membership in an org, making a real org they can't see indistinguishable from a fake org id; 403 is reserved for a real member missing a specific permission. Added a cross-org isolation test suite and a filesystem-scanning guard test that fails CI if a future org-scoped route skips the check without a justified exemption. Merged as PR #11 after re-verifying locally (an earlier run's CI stall had cleared by the time this run checked). apps/api's `PermissionGuard` still returns a single 403 for both cases, but apps/api has no org-scoped routes yet (KAN-24 left `request.principal` unpopulated) so there's nothing to enumerate there today — flagged as a follow-up once apps/api gains real org-scoped endpoints. |
| KAN-27 | E1.7 Org Resource Library: shared credentials (attach per project + ad-account slicing), templates, people registry, attach/detach with approval | 0 | 2 | done | Delivered for the surfaces buildable today: `SharedCredentialModel` (identity + org-level available-scope slice, no OAuth secret storage yet — that's KAN-29/KMS, not built), `ResourceTemplateModel` (versioned, copy-with-link + version-pin on attach), `OrgPersonModel` (people registry), all joined by one `ResourceAttachmentModel` driving request → approve/reject → detach. New `resources.manage` permission (org-scoped roles only); `project.manage` gates the project-admin-initiated request half. New `apps/web` routes + an org resource-library page and a per-project resources page. Independent review + fixes before merge (see PROGRESS.md); e2e-covered end to end. |
| KAN-28 | E2.1 Key service: mint per project+env (gos_live_/gos_test_), scope list, hashed storage, last-used tracking | 0 | 2 | done | Delivered in `packages/firebase-orm-models`: `ApiKeyModel` (project+environment scoped, stores only a SHA-256 hash of the raw key) and `key.service.ts` (`mintApiKey`/`verifyApiKeyForRequest`/`revokeApiKey`/`listApiKeysForProject`). `packages/shared` gained `apiKeyModeForEnvironment()` and a curated `API_KEY_SCOPES` least-privilege catalog. No admin UI (that's KAN-30, which depends on this) and no route wiring yet (no ingest API exists until KAN-32) — both deliberately deferred, same pattern as KAN-23. |
| KAN-29 | E2.2 KMS envelope encryption for OAuth tokens/secrets (vault module) | 0 | 2 | done | Delivered in `packages/firebase-orm-models/src/vault` (envelope encryption: random per-secret DEK + AES-256-GCM, DEK wrapped by a `KmsProvider`; `LocalKmsProvider` stands in for real GCP Cloud KMS until KAN-18) plus `vault.service.ts` wiring a new `encrypted_secret` field into KAN-27's `SharedCredentialModel`. Admin UI: write-only set/rotate-secret form on the org resource library page, gated on `resources.manage`. A duplicate unmerged PR (#14, from an earlier run against the same stale "todo" status) was closed as superseded once this landed — see PROGRESS.md. |
| KAN-30 | E2.3 Admin UI: keys page (create with scope picker, copy-once, revoke, last-used) | 0 | 2 | done | Delivered: `orgs/:orgId/projects/:projectId/keys` page — mint scoped to one environment with a least-privilege scope checkbox picker, list every key (active/revoked) with prefix + last-used, revoke immediately. Whole feature (page + both routes) gated on `keys.manage`, stricter than KAN-27's any-member-can-browse pattern. New `listEnvironmentsForProject` query in firebase-orm-models. Audit-log entries deliberately not written (KAN-44 doesn't exist yet), same "buildable today" split as KAN-23/KAN-27. |
| KAN-31 | E3.1 Schema Registry: SchemaDef models (entity/event/measure; fields, types, PII flags, identity keys), versioning | 0 | 2 | done | Delivered in `packages/firebase-orm-models`: `SchemaDefModel` (one document per version, project-scoped) + `schema-registry.service.ts` (`registerSchemaDefinition`/`evolveSchemaDefinition` with non-breaking-evolution validation — removed/retyped/tightened/dropped-identity-key fields rejected — /`listSchemaDefinitionsForProject`/`getActiveSchemaDefinition`). Prior versions are kept (`status: superseded`), never mutated/deleted, so all stay queryable. Admin UI: project-scoped schema-defs page (register + evolve with a field builder), gated on `schema.write`. Not transactional — a known, documented concurrency gap (see PROGRESS.md) deferred as bigger than this story. |
| KAN-32 | E3.2 POST /v1/ingest/(events|entities|measures): batch validation, idempotency, 202 + batch_id, per-record results | 0 | 2 | done | Delivered: flat `/v1/ingest/(events\|entities\|measures)` + `GET /v1/ingest/batches/{id}` in `apps/api`, authenticated by a new `authenticateApiKey` (KAN-28) that resolves org/project/environment from the key's own hash rather than the URL. `packages/firebase-orm-models` gained `IngestBatchModel`/`IngestDedupKeyModel` + `ingest.service.ts` (schema validation against KAN-31's registry, content-hashed dedup keyed by environment+kind+schema-name+client-id). Load-test half of the AC deferred (no staging infra yet, KAN-18). A duplicate PR (#18, from an independent same-day run) was closed as superseded — see PROGRESS.md. |
| KAN-33 | E3.3 Pipeline: accepted records -> Pub/Sub -> BigQuery raw tables (partitioned by org/project/env/date) | 0 | 3 | done | Delivered: a provider-agnostic publish/land pipeline in `packages/firebase-orm-models` — `PipelineMessageModel` (Firestore outbox standing in for a real Pub/Sub topic) and `RawRecordModel` (Firestore raw-table stand-in, `partition_date` field standing in for the real `org/project/env/date` BigQuery partition), both until KAN-18 provisions a real GCP project. `ingest.service.ts`'s `ingestBatch` now publishes every accepted record's full raw payload and lands it (`landPipelineMessages`), scoped to just that batch — closing the gap KAN-32's own entry flagged ("accepted records aren't yet landed anywhere durable"). `drainPendingPipelineMessages` is a separate, explicitly environment-wide catch-up sweep for a future worker (KAN-38), not on the ingest path. Load-test half of the AC still deferred (no staging infra, KAN-18); the "<60s visible" half is met trivially since landing is synchronous in this buildable-today version. |
| KAN-34 | E3.4 Quarantine + DLQ + replay API; per-key rate limiting (Redis token bucket, 429 + Retry-After) | 0 | 3 | done | Delivered: new `QuarantinedRecordModel` persists a quarantined record's raw payload (previously only validation status was stored); `quarantine.service.ts`'s `replayQuarantinedRecord` re-validates against the current active schema and accepts/dedupes/re-quarantines. Pipeline DLQ: `listFailedPipelineMessagesForProject`/`replayFailedPipelineMessagesForProject` retry a project's `failed` pipeline messages. Per-key rate limiting: a new provider-agnostic `rate-limit/` module (`InMemoryTokenBucketRateLimiter`, standing in for Redis until KAN-18) wired into `ApiKeyAuthGuard` — 429 + `Retry-After` once a key's bucket is exhausted. Admin UI: the KAN-35 ingest-health page's quarantine browser now reads from the durable store (stable ids) with a Replay button, plus a new pipeline-delivery-failures section with a retry-all action. |
| KAN-35 | E3.5 Admin UI: ingest health (throughput, error rate, freshness), quarantine browser + replay button | 0 | 2 | done | Delivered: a project-scoped `orgs/:orgId/projects/:projectId/ingest-health` page reading KAN-32's `IngestBatchModel` — overall + per-kind rollups (total/accepted/quarantined/duplicate, error rate computed from quarantined only, freshness, throughput), plus a quarantine browser (client id, kind, environment, reasons). Gated on `ingest.write`. Replay deliberately not built — `record_results` still has no raw payload to resubmit (that gap is now closed by KAN-33, but this story didn't re-scope to add replay). New `listRecentIngestBatchesForProject` (first ordered/limited Firestore query in this codebase). |
| KAN-36 | E3.6 Per-event volume sparklines + tracking-broke alerts (volume anomaly per event) | 1 | - | done | Delivered: `TrackingAlertModel` (append-only-episode, "current = newest" convention) + `tracking-alert.service.ts` — `checkTrackingAlertsForProject` (manually-triggerable "check now", KAN-38's buildable-today stand-in until KAN-18) fires/refreshes/resolves a per-event silence episode against a 1-hour threshold; `getEventVolumeOverviewForProject` computes a 7-day daily-bucketed sparkline fresh from bounded `RawRecordModel` reads. Admin UI: extends the Schema Registry page (the plan doc's own placement) with an "Event volume & tracking alerts" section, gated on the page's existing `schema.write` permission. Self-review found and fixed a real bug (ascending-order truncation made busy/healthy events show a stale last-seen) plus an off-by-one in day-bucketing and a duplicate Firestore read.
| KAN-37 | E4.1 dbt project: staging models over raw ingest, canonical entities/events/measures core tables, dbt tests | 0 | 3 | done | Delivered: `packages/dbt-transform/` — a dbt project run against dbt-duckdb (buildable-today stand-in until KAN-18 provisions BigQuery). Staging models (`stg_raw_records` split by kind) over a fixture `raw_records` seed standing in for a real export of KAN-33's Firestore collection. Canonical core tables: `entities` (current-state snapshot), `events` and `measures` (both append-only). dbt tests (not_null/unique/accepted_values + a singular non-negative-measure-value business rule) all green (`dbt build`, 42/42). `pnpm build`/`pnpm test` self-provision a pinned Python venv, no separate CI step needed beyond a Python setup + cache step. No admin UI — internal data-transformation machinery, not human-manageable. |
| KAN-38 | E4.2 Orchestration (Dagster/Cloud Workflows): scheduled runs per project, freshness metadata written back | 0 | 3 | done | Delivered: `OrchestrationRunModel` (Firestore run-record history) + `triggerOrchestrationRun`/`listOrchestrationRunsForProject` in `packages/firebase-orm-models` — a manually-triggerable "run once" stand-in for a real scheduler, deferred until KAN-18. `LocalDbtOrchestrationExecutor` actually shells out to a real dbt build (KAN-37) and reads back per-table row counts/latest timestamps as freshness metadata. Admin UI: a new Orchestration section on the KAN-35 ingest-health page (current freshness, run history, a Run now button), gated on the existing `ingest.write` permission. Found and fixed a real bug during manual verification: `require.resolve`/`__dirname`-based path lookup for the sibling `dbt-transform` package silently broke under a real `next dev` server (Next's webpack bundling rewrites both), fixed via a `process.cwd()`-based monorepo-root walk. |
| KAN-39 | E4.3 Cost guardrails: per-project BigQuery quotas/labels, query cost logging | 0 | 3 | done | Delivered: `ProjectCostQuotaModel` (append-only quota+labels config, "current = newest") + `QueryCostLogEntryModel` (append-only, one entry per non-cache-hit `queryMetrics` call) in `packages/firebase-orm-models`, plus `cost-guardrail.service.ts` (`getProjectCostQuota`/`setProjectCostQuota`/`checkProjectQueryQuota`/`recordQueryCostLogEntry`/`listQueryCostLogEntriesForProject`). `queryMetrics` now enforces the quota before a cache-miss reaches the `WarehouseQueryExecutor` and best-effort logs every non-cache-hit attempt. `apps/api`'s `MetricsController` maps `ProjectQueryQuotaExceededError` to 429. Admin UI: new project-scoped Cost guardrails page (usage vs. limit, set-quota form, cost log), gated on the existing `project.manage` permission. This closes out every sprint-3 `todo`. Follow-up closed 2026-08-20 (PR #116): `estimated_cost_usd` is real now that KAN-18's BigQuery warehouse is live — `BigQueryWarehouseQueryExecutor.executeWithStats()` estimates cost from the real `totalBytesProcessed` at BigQuery's on-demand $6.25/TiB, threaded through `queryMetrics` via a new optional `WarehouseQueryExecutorWithStats` capability (every other executor is unaffected, still logs `null`); the cost-guardrails page now actually renders the per-entry estimate (it never had before). A tuple-index bug in `createRealBigQueryClient` (reading the SDK's `nextQuery` slot instead of the real `apiResponse` slot, which would have kept the estimate `null` forever) was caught and fixed during this run's own pre-merge self-review — see PROGRESS.md. |
| KAN-40 | E5.1 Metric definition format (YAML/Firestore): aggregations, formulas, dimensions, filters + validation | 0 | 3 | done | Delivered: `MetricDefModel` (project-scoped, one doc per version) + `metric-registry.service.ts` (`registerMetricDefinition`/`evolveMetricDefinition`/`listMetricDefinitionsForProject`/`getActiveMetricDefinition`), mirroring KAN-31's schema-registry versioning (v1 -> v{n+1}, previous kept `superseded`). Validates aggregation function/table/column/filters, formula syntax, self-reference, unknown-metric references, and a transitive circular-dependency check across evolutions. Admin UI: project-scoped metric-defs page + register/evolve routes, gated on the existing `metrics.write` permission. Compiler (KAN-41) and query API (KAN-42) not built yet — out of this story's scope. |
| KAN-41 | E5.2 Compiler: definition + query request -> BigQuery SQL (time grain, compare-period, breakdown) | 0 | 3 | done | Delivered: `packages/shared/src/metrics-compiler` — a pure, Firestore-free `compileMetricQuery(catalog, request)` compiling to BigQuery SQL (`DATE_TRUNC` time-grain bucketing, dimension breakdown, `compare` via `UNION ALL` with a `period` column, formulas parsed into an AST so `/` compiles to `SAFE_DIVIDE`, filter/time values as bound `@params`). 10 golden-file SQL fixtures per the AC. `packages/firebase-orm-models` gained `compileMetricQueryForProject`, resolving KAN-40's registered definitions (recursively through nested formulas) and returning `definitionRefs`. `MetricAggregationDef` gained a required `timeColumn` (plumbed through registry validation + the metric-defs admin form) since the canonical fact tables don't share one time-column name. `POST /v1/metrics/query` (KAN-42) not built — compiler-only, per the existing plan split. |
| KAN-42 | E5.3 POST /v1/metrics/query + GET /v1/metrics catalog + Redis result cache | 0 | 3 | done | Delivered: `MetricsController` (`apps/api`) — bearer-key-authenticated `POST /metrics/query`, `GET /metrics` (catalog), `GET /metrics/:name` (definition + lineage), reusing the existing `metrics.write` API key scope (no separate read permission exists in the catalog). `packages/firebase-orm-models` gained `queryMetrics()` (compiles via KAN-41's `compileMetricQueryForProject`, then a provider-agnostic `WarehouseQueryExecutor` — defaults to a `NotConfiguredWarehouseQueryExecutor` until KAN-18 (BigQuery) + KAN-37 (dbt fact tables) exist) and an in-memory `MetricQueryResultCache` (a Redis stand-in, keyed by org+project+definition-versions+params) until KAN-18 provisions real Redis. A follow-up run reconciling a duplicate parallel implementation also hardened `packages/shared`'s compiler itself against an unvalidated filter `operator` value being spliced into SQL (defense in depth alongside `metrics-request.ts`'s own boundary check) — see PROGRESS.md. |
| KAN-43 | E6.1 Apply for Google Ads developer token + Meta app & Marketing API review (LONG LEAD - submit week 1!) | 0 | 1 | needs-human | LONG LEAD - Google Ads dev token + Meta app/Marketing API review must be submitted by a human in week 1. |
| KAN-44 | E6.2 Audit log service (append-only): every config/key/role/schema change | 0 | 3 | done | Delivered: `AuditLogEntryModel` (org-scoped, `organizations/:organization_id/audit_log_entries`) + `audit-log.service.ts` (`recordAuditLogEntry`/`listAuditLogEntriesForOrg`/`verifyAuditLogChainForOrg`) — an append-only sha256 hash chain per org (each entry commits to the previous entry's hash), giving "tamper-evident" a concrete, verifiable meaning. Wired into key mint/revoke, schema register/evolve, invite-accept (role grant), member/invite removal, quarantine replay, and pipeline DLQ replay — the "key/role/schema" surfaces that existed at pick time. Admin UI: new `orgs/:orgId/audit-log` page + `GET .../audit-log` route, gated on a new `audit.read` permission (withheld from API keys and from `project_admin`, granted to `org_owner`/`org_admin`/`platform_admin`). Vault secret set/rotate and resource-attachment request/approve/reject/detach are now wired too (PR #104, hardened in a same-day follow-up — see PROGRESS.md) — every config/key/role/schema-change surface named in the AC is covered. |
| KAN-45 | E6.3 i18n scaffold (next-intl), en+he resource files, RTL layout toggle | 0 | 1 | done | Delivered in apps/web: locale-prefixed routing ([locale] segment + middleware), en/he messages, RTL `dir` toggle, LocaleSwitcher UI, and an eslint `react/jsx-no-literals` rule enforcing "no hard-coded UI strings". |
| KAN-46 | E7.1 plugin.yaml manifest parser + registry storage + install-per-project flow (scope consent) | 1 | 4 | done | Delivered: `packages/shared`'s pure `parsePluginManifest` (plugin.yaml -> validated shape, plan `12 §5`). `packages/firebase-orm-models` gained `PluginManifestModel` (org-scoped, immutable per plugin_id+version — package-registry style, not the schema/metric registries' single-active-version convention) + `PluginInstallModel` (project-scoped, installed/disabled/uninstalled lifecycle) + `plugin-registry.service.ts` (register/list/get manifests; install with exact scope-consent match + config_schema validation; disable/enable/uninstall). Admin UI: org-scoped Plugin registry page + project-scoped Plugins page, both gated on the existing `plugin.install` permission. KAN-47 (source-plugin runtime) and KAN-48 (polished plugin gallery UI) build on this. |
| KAN-47 | E7.2 Source-plugin runtime: scheduled execution, scoped short-lived creds, cursor persistence, retry/backoff | 1 | 4 | done | Delivered: `PluginSourceRunModel` (run history) + `PluginInstallModel.source_cursor` (persisted cursor, "survives restart") + a new `plugin-runtime/` module (`mintPluginRuntimeCredential`, `runWithRetryBackoff`, `SourcePluginExecutor` interface, `ToyCounterSourcePluginExecutor`) + `plugin-runtime.service.ts`'s `triggerSourcePluginRun` (manual "run once" stand-in for a real Cloud Run job scheduler, same posture as KAN-38's orchestration runs) — hands produced records to the existing `ingestBatch` pipeline; cursor only advances once sync + landing both succeed. Admin UI: new "Source runtime" section on the project Plugins page (environment picker + Run now + run history), gated on the existing `plugin.install` permission. KAN-48 (polished plugin gallery/health UI) builds on this. |
| KAN-48 | E7.3 Admin UI: plugin gallery, config forms rendered from config_schema, per-plugin health | 1 | 4 | done | Delivered: `InstallPluginForm` now a browsable card gallery (grouped by plugin id) with typed config-form widgets (real checkbox for booleans, inline required-field validation) instead of the old raw dropdown/text-input form; new `pluginInstallHealth`/`pluginInstallHealthLabelKey` view-mappers + `PluginHealthSummary` component show healthy/degraded/running/never-run for `source`-type installs (derived from run history) or plain lifecycle status otherwise, above the existing (now collapsible) run-history list. |
| KAN-49 | E8.1 Stripe plugin: OAuth/keys, backfill + webhooks (charges, invoices, subscriptions, refunds, failed payments) -> commerce schemas incl. mrr_normalized | 1 | 4 | done | Delivered (PR #35, a parallel session's implementation, merged after this run's independent review): `StripeSourcePluginExecutor` (KAN-47 `SourcePluginExecutor`) backfills via Stripe's List Charges/Invoices/Refunds/Subscriptions endpoints (alternating events/entities phases, `starting_after` pagination -> `created[gte]` incremental polling once caught up), fed by a provider-agnostic `StripeApiClient` (`StripeHttpApiClient` real implementation, fake-client-tested). A new `.../plugins/[installId]/environments/[environmentId]/stripe-webhook` route (session-less, HMAC-`Stripe-Signature`-authenticated, justified `route-isolation-guard.test.ts` exemption) lands real-time events through the same path. Commerce schemas (`stripe_charge`/`stripe_invoice`/`stripe_refund`/`stripe_failed_payment` events + `stripe_subscription` entity incl. `mrr_normalized`) auto-register idempotently on first "Run now". Keys (not OAuth — Stripe Connect needs a human-submitted platform app, KAN-43-style, deferred) ride through the existing KAN-27/29 Resource Library credential vault (`stripe` added to `CREDENTIAL_PROVIDERS`) rather than a new secret-storage mechanism. |
| KAN-50 | E8.2 Google Ads plugin: OAuth, GAQL daily reports (campaign/adgroup/ad), currency normalization, backfill 13mo | 1 | 5 | blocked-by | blocked-by KAN-43 (needs Google Ads API approval). |
| KAN-51 | E8.3 Meta Ads plugin: OAuth, Insights (campaign/adset/ad), creative metadata pull, backfill | 1 | 6 | blocked-by | blocked-by KAN-43 (needs Meta Marketing API approval). |
| KAN-52 | E8.4 GA4 plugin (BigQuery export or Data API): sessions, events, UTM/click-id capture | 1 | 6 | done | Delivered (PR #40). Took the Data API path (not BigQuery export, which needs KAN-18); captures UTM-equivalent acquisition dimensions (source/medium/campaign/channel-group), not raw click ids — those need GA4's BigQuery export, deferred. Not blocked-by KAN-43 (GA4 Data API doesn't need Google's Ads-style developer-token approval, per `docs/plan/13-task-breakdown.md`'s own note that KAN-43 gates only E8.2/E8.3). |
| KAN-53 | E9.1 Per-project hook endpoints: store raw payload, signature verification, review queue | 1 | 6 | done | Delivered (PR #41). |
| KAN-54 | E9.2 Mapping engine: saved field-mappings (JSONPath -> schema fields), transforms, test-run on sample | 1 | 6 | done | Delivered (PR #43): a pure JSONPath-to-schema-field engine in `packages/shared/src/mapping-engine` (rename/cast/template/static transforms) + `FieldMappingModel`/`field-mapping.service.ts` (create/list/disable/testRun, reusing `ingest.service.ts`'s envelope/schema validators) + a project-scoped admin page in `apps/web`. Follow-up closed 2026-08-20: `applyFieldMappingToDelivery` (shares its mapping/validation path with `testRunFieldMapping` via a new `runFieldMapping` helper) now lands a clean mapping through the real ingest pipeline (KAN-32/33) and marks the delivery `reviewed`+applied (`applied_at`/`applied_by`/`applied_field_mapping_id`/`applied_batch_id` on `HookDeliveryModel`) — an "Apply to delivery" action next to the existing test-run panel. |
| KAN-55 | E9.3 Mapping UI with AI-assisted suggestion (LLM proposes mapping from sample payload; user confirms) | 1 | 7 | done | Delivered (PR #46). Proposer is a deterministic name/type-similarity heuristic (buildable-today stand-in for a real LLM call) — see PROGRESS.md. |
| KAN-56 | E10.1 Deterministic identity stitching (dbt): registered identity keys -> bridge_identity, conflict rules | 1 | 5 | done | Delivered (PR #37): new `bridge_identity` core dbt model, resolving each anonymous `touchpoint`-kind client_id to a customer identity via registered identity-key evidence (`seeds/schema_identity_fields.csv`, a stand-in for a warehouse export of `SchemaDefModel.field_defs.is_identity_key` — same posture `raw_records.csv` establishes for the raw ingest table). Two link types: a direct `anon_id_cooccurrence` (a record's own payload declares `anon_id`) and `shared_key:<field_name>` (an anon-side and customer-side record share another registered identity key's value). Conflict rule: lowest precedence wins regardless of disagreeing weaker evidence; ties broken by earliest evidence then `customer_id`; `is_conflicted` stays true whenever more than one candidate existed even after resolution (audit trail); `confidence` drops to 0.5 only when the winner itself needed a tie-break among purely shared-key evidence. Identity-key registration already has an admin surface (KAN-31's Schema Registry `is_identity_key` checkbox) — no new UI needed. `proj_2` synthetic fixture rows (isolated from `proj_1`/`proj_9`'s exact-count-asserting tests) cover the AC's anon->signup->purchase journey plus every conflict-rule branch; a self-review before merge found and fixed a real bug where the winning precedence and reported `resolved_at` could come from two different links for the same pair — confirmed via a regression fixture that genuinely fails against the pre-fix model. |
| KAN-57 | E10.2 Touchpoint capture: JS snippet/SDK storing UTM/click-ids at entry, attached to ingest events | 1 | 5 | done | Delivered (PR #38): `packages/shared/src/touchpoint-capture` (pure UTM/click-id parsing incl. gclid/msclkid/fbclid/ttclid, cross-site-referrer-gated channel classification, touchpoint + tracked-event payload builders, schema field defs matching KAN-56's `touchpoint`-kind-event convention); `ensureTouchpointSchemaRegistered` in `packages/firebase-orm-models` (idempotent register-if-missing) plus an emulator test landing a real gclid through the ingest API end-to-end and linking a follow-up event back via `anon_id`; a new `packages/tracking-sdk` package (`createTracker` — page/track/identify/getAnonId — and a self-contained embeddable `<script>` snippet, both firing the entry touchpoint on whichever call happens first). Admin UI: the Keys page shows a copyable embed snippet right after minting an `ingest.write`-only key (never offered for a multi-scope key); the Schema Registry page gets a one-click "set up touchpoint capture" action. Independently reviewed via 5 parallel finder passes before merge — see PROGRESS.md for the real bugs found and fixed. |
| KAN-58 | E10.3 Last-touch + first-touch attribution models; fact_attribution | 1 | 5 | done | Delivered (PR #39). New core dbt model `fact_attribution`: rules-based first-touch/last-touch credit for every customer-side (non-`touchpoint`) event, resolved back to `bridge_identity` (KAN-56)-linked touchpoints at-or-before the conversion; conversions with no attributable touchpoint get an explicit `unattributed` row per model rather than being dropped. `channel_id`/`campaign_id` are the touchpoint's raw `channel`/`utm_campaign` strings (no dimension table yet), the vocabulary a future `troi`/`cac`-by-channel metric (KAN-59) would join against. |
| KAN-59 | E11.1 SaaS/marketing metric-pack plugin: ad_spend, signups, cost_per_signup, cac, conversion_to_paying, mrr, mrr_movements, net_mrr_churn, troi, collected_revenue, failed_charge_rate | 1 | 7 | done | Delivered (PR #48). Unblocks KAN-61 (default boards). **Follow-up fixed 2026-08-24 (PR #265):** `collected_revenue`'s aggregation (`saas-metric-pack/metrics.ts`) had filtered `fact_revenue_event` on `status = 'succeeded'` only, with no `type` filter — `fact_revenue_event.sql` emits a synthetic `type = 'first_charge'` row alongside the real `type = 'charge'` row for each customer's first payment, so the metric double-counted every customer's first successful charge. Found during KAN-88's PR #263 (closed as superseded) but deliberately left for its own reviewed PR rather than fixed as a drive-by there; PR #265 added the missing `{ field: 'type', operator: '=', value: 'charge' }` filter, mirroring `fact_customer_payback.sql`'s identical existing fix for the same table. |
| KAN-60 | E11.2 Dashboard framework: board model, grid drag-drop, tile types (line/bar/big-number/table/funnel), metric picker, date range + compare, global filters | 1 | 4 | done | Delivered (PR #36, a parallel session's implementation, merged after this run's independent review — see PROGRESS.md). `BoardModel` (project-scoped, embedded `tiles` array) + `board.service.ts` (create/list/get/update-settings/save-tiles/delete + `queryBoardTile`, which composes a tile's own metric picker/dimensions/filters/compare-override with the board's date range/grain/compare/global-filters into a `MetricQueryRequest` run through the existing KAN-41/42 `queryMetrics` pipeline, degrading per-tile rather than blanking the whole board when the warehouse isn't configured yet). Rejects a tile referencing a metric that isn't currently registered+active in the project before the tile is even saved. Admin UI: project-scoped Boards list + board detail page — a 12-column CSS grid with native HTML5 drag-and-drop tile repositioning, resize, remove, a metric-picker sourced only from the project's active metric catalog, and board-level date range/compare/global-filters editors — gated on the existing `dashboards.write` permission. `line`/`bar`/`funnel`/`table`/`big_number` tiles render from a pure view-mapper with no charting-library dependency (same posture KAN-36's sparkline established). Follow-up closed 2026-08-20 (PR #110): `updateBoardSettings`/`saveBoardTiles`/`deleteBoard` now audit-log too (`board.settings_update`/`board.tiles_save`/`board.delete`), matching every sibling in-place-update surface. |
| KAN-61 | E11.3 Default boards shipped with pack: Marketing, Revenue/MRR, Funnel | 1 | 5 | done | Delivered (PR #49, a parallel session's implementation, merged after this run's independent review — see PROGRESS.md). |
| KAN-62 | E11.4 Cohort engine v1 + heatmap tile (signup-month x conversion/retention) | 1 | 6 | done | Delivered (PR #44). |
| KAN-63 | E11.5 Engagement pack: dau/wau/mau, stickiness ratio, L28/LN histogram + histogram tile type | 1 | - | done | Delivered (PR #53). |
| KAN-64 | E12.1 Goal model (metric, target, deadline, owner, direction min/max/range, work-week/weekend rhythm) + progress + pace projection | 1 | 6 | done | Delivered (PR #45). |
| KAN-65 | E12.2 Win rules engine: event pattern -> win, realtime path (ingest -> Pub/Sub -> WebSocket) | 1 | 7 | done | Delivered (PR #50, a parallel session's implementation, merged after this run's independent review). |
| KAN-66 | E12.2b Win catalog: reactivation + trial-conversion win types; trial-pipeline war-room widget | 1 | - | done | Delivered (PR #54, a parallel session's implementation, merged after this run's independent review). |
| KAN-67 | E12.3 War-room TV mode: fullscreen rotation, win feed overlay, confetti + sound per win type, device pairing code, reduced-motion | 1 | 7 | done | Delivered (PR #55, a parallel session's implementation, merged after this run's independent review — see PROGRESS.md). |
| KAN-68 | E13.1 Onboarding wizard: org/project -> pack pick -> connect sources or push-your-own (curl+key) -> AI-proposed funnel mapping -> starter board | 1 | 7 | done | Delivered (PR #56). Project creation now redirects into the wizard instead of the org page. |
| KAN-69 | E13.2 Freshness badges + degraded-state UX on every tile; empty states | 1 | 7 | done | Delivered (PR #57). |
| KAN-70 | E13.3 Alpha feedback instrumentation: dogfood our own funnel via our Ingest API | 1 | 7 | done | Delivered (PR #58, a parallel session's implementation, merged after this run's independent review). |
| KAN-71 | E21.1 Automation-service action pipeline: dry-run diff -> approval -> execute -> verify -> rollback; guardrail policy engine; kill switch | 3 | - | done | Delivered (PR #59). Buildable-today stand-in: a `SimulatedAdAccountExecutor` mutates a new `AutomationTargetStateModel` (simulated ad-campaign state) instead of a real Google/Meta Ads account until KAN-72/73 exist. Org-scoped kill switch only (no platform-wide one yet — no platform-level admin surface exists in this app today). |
| KAN-72 | E21.2 Google Ads Manage plugin: campaign/ad-group/ad create+edit (RSA/PMax), keywords/negatives, default-paused creation | 3 | - | done | Delivered a real vertical slice against KAN-71's pipeline: two new action types (`campaign_draft_create`, `campaign_activation`), a real Google Ads REST API client + OAuth2 credential flow + `GoogleAdsAutomationActionExecutor`, Manage-tier-only write-tier gating, and admin UI (propose-draft form + activate button). PMax asset groups, post-creation ad/keyword edits, and audience attach are explicitly deferred/documented — see PROGRESS.md. **Follow-up delivered 2026-08-25 (PR #288):** the "audience attach" half of that deferred bullet — the direct Google Ads sibling of KAN-73's own Meta Custom Audience follow-up (PR #286). A new built-in action plugin (`com.growthos.google-customer-match`) syncs a saved segment's currently-matching members to a Google Ads Customer Match user list (SHA-256-hashed email, contact-info based), riding the same generic `crm-sync.service.ts`'s `syncSegmentToCrm` mechanism (extended to dispatch to a new `GoogleCustomerMatchSinkPluginExecutor`) and this story's own Google Ads credential resolution — no new credential type. Two new `GoogleAdsApiClient` methods implement the real upload flow (`userLists:mutate` create, then the offline-user-data-job create/addOperations/run sequence Google's Customer Match API requires, unlike Meta's single endpoint). `PluginInstallModel`'s Meta-specific `meta_custom_audience_id` field was generalized to a connector-agnostic `sink_external_ref` so both connectors share the one "cache the created external id, reuse on repeat sync" field. The same retry-orphan bug class KAN-73's own follow-up fixed was pre-empted here (external id cached as mutable executor-instance state before this PR ever merged, with a regression test). Lookalike/Similar Audience expansion and non-email identifiers remain deferred, documented. |
| KAN-73 | E21.3 Meta Manage plugin: campaign/adset/ad create+edit, creative upload, Custom/Lookalike audience creation from GrowthOS segments | 3 | - | done | Delivered (PR #69, a parallel session's implementation, merged after this run's independent review). Generalized `CampaignDraft` into a `platform`-discriminated union (`GoogleAdsCampaignDraft \| MetaCampaignDraft`) so KAN-71's existing `campaign_draft_create`/`campaign_activation` action types serve both platforms with no parallel action-type/pipeline fork; a real `MetaAdsHttpApiClient` (Graph API v21.0) + `MetaAutomationActionExecutor` + credential/manifest plumbing mirror KAN-72's Google Ads plugin; `apps/web`'s campaign-draft form gained a platform toggle instead of a duplicate form. Real Meta creative image upload, Custom/Lookalike audience creation from a GrowthOS segment, and post-creation edits beyond activation are explicitly deferred — no "segment" concept with live membership exists yet to build real audience upload against. **Follow-up delivered 2026-08-25 (PR #286):** the Custom Audience half of that deferred bullet — KAN-81's `listSegmentMembers` had since closed the "no live segment membership" gap this row itself named. A new built-in action plugin (`com.growthos.meta-custom-audience`) syncs a saved segment's currently-matching members to Meta as a SHA-256-hashed-email Custom Audience, riding the existing generic "sync a segment to an action-type plugin install" mechanism (`crm-sync.service.ts`'s `syncSegmentToCrm`, extended to dispatch to a new `MetaCustomAudienceSinkPluginExecutor` alongside the existing CRM webhook one) and this story's own Meta Ads credential resolution — no new credential type. The created audience id is cached on `PluginInstallModel.meta_custom_audience_id` so repeat syncs add to the same audience. Self-review before merge caught and fixed a real bug: the executor's `existingAudienceId` was only ever read from the constructor, so a transient failure between audience-creation and email-upload would create a second, orphaned audience on the automatic retry (since `syncSegmentToCrm` retries the *same* executor instance) — fixed by caching the created id as mutable instance state, with a regression test. Lookalike Audience creation remains deferred, documented. |
| KAN-74 | E21.4 Admin: write-tier selector per connection (Read/Optimize/Manage), guardrail policy editor, action-history UI with before/after | 3 | - | done | Delivered (PR #61). Guardrail policy editor + action queue already existed from KAN-71; this story added the write-tier field on `ResourceAttachmentModel`, gating automation propose/approve/execute on it (immediate revocation on downgrade), and a generic before/after diff view. |
| KAN-75 | E22.1 MCP server (Streamable HTTP), per-project scope, OAuth 2.1 + scoped keys; read tools: query_metric, compare_periods, decompose, funnels/cohorts, search_customers, list_insights | 3 | - | done | Delivered (PR #62). Unblocks KAN-76 (act tools). Follow-up closed 2026-08-19: `query_funnel` now built — a new `fact_funnel_step` dbt core model (one row per customer per funnel stage first reached, against a `funnel_step_mappings` seed standing in for a warehouse export of KAN-68's `OnboardingStateModel.funnel_steps`) backs a new `queryProjectFunnelSteps` adapter and `query_funnel` MCP tool (per-stage distinct-customer counts + conversion rate off the first step), same posture as `query_cohort`/`fact_cohort_retention`. |
| KAN-76 | E22.2 Act tools: propose_action (dry-run diff) / approve_action (requires automation.approve), create_goal, create_segment | 3 | - | done | Delivered (PR #63, a parallel session's implementation, merged after this run's independent review). Follow-up closed 2026-08-20 (PR #111): a saved segment had no execution path — `countSegmentMembers` (`segment.service.ts`) now computes a live member count against the `entities` core table, typed per the segment's active schema fields, degrading cleanly (not crashing) when the warehouse isn't configured. Segments page shows a member-count badge per segment. `isValidSegmentFilterCondition` also tightened to reject an unsafe filter field name at save time, so a saved segment can never fail to count later. |
| KAN-77 | E22.3 MCP surface in isolation test suite + audit logging (principal + client identity) + rate/token budgets per key | 3 | - | done | Delivered (PR #64). Picked up a complete WIP implementation already on `origin/kan-77-mcp-isolation-audit-ratelimit` from an earlier run in this session; independently reviewed + fully verified before opening the PR. |
| KAN-78 | E22.4 Docs + example clients: Claude Desktop config, claude.ai connector setup, headless-agent recipe | 3 | - | done | Delivered: `docs/mcp/README.md` (connect-in-under-10-minutes guide covering Claude Desktop, claude.ai custom connectors, and a headless-agent recipe, plus a tool reference/safety/troubleshooting section) + two example `claude_desktop_config.json` snippets (native OAuth remote connector, and an `mcp-remote`-bridged API-key variant) + a new runnable, tested `packages/mcp-headless-example` package (`connectGrowthOsMcpClient`/`fetchWeeklyMetricDigest` library + a `growthos-mcp-weekly-digest` CLI) demonstrating the plan's own "every Monday my agent pulls last week's CAC" example against the real `@modelcontextprotocol/sdk` client. This closes out Epic E22 (MCP). |
| KAN-80 | E14.z Billing-ops feeds: recent charges / failed charges / refunds as a browsable record feed (`docs/plan/14-gap-analysis.md` Gap 5+15) | 2 | 8 | done | Delivered: a project-scoped Billing ops feed admin page reading the same landed `RawRecordModel`s KAN-36's event-volume sparkline already reads, scoped to the Stripe connector's (KAN-49) `stripe_charge`/`stripe_failed_payment`/`stripe_refund` event schemas, newest-first, folded across every environment. `packages/firebase-orm-models` gained `listRecentBillingEventsForProject` (one bounded per-schema Firestore query, merged+re-sorted client-side, same "whole project, one admin view" posture as `listRecentIngestBatchesForProject`) + `BILLING_OPS_FEED_EVENT_SCHEMA_NAMES`. Gated on `ingest.write` (no dedicated billing-read permission exists; the codebase's own established convention is gating a project-scoped read surface on the nearest write permission — see `packages/shared/src/policy/permissions.ts`'s doc comment). Dunning status (the gap doc's stretch goal) and the CRM-sync/saved-segment-as-worklist half of Gap 5 are out of scope — no subscription-lifecycle/dunning model or record-feed-as-segment mechanism exists yet to build either against; that's KAN-81 (E14.x) territory. |
| KAN-81 | E14.x Ops Lists & Feeds: live record feeds (payments/churn/failed), saved segments with owners + statuses, CRM-sync action plugin, AI-suggested lists (`docs/plan/14-gap-analysis.md` Gaps 5+9) | 2 | - | done | ~10d per the gap doc's own estimate; too large for one run, split into buildable-today slices (same posture KAN-18/KAN-27/KAN-46-48 established). Slice 1 (PR #233, done): saved segments become work lists — owner assignment (`owner_person_id` -> `OrgPersonModel`) + status ticking (`open`/`in_progress`/`done`), with a `PATCH .../segments/[segmentId]` route and admin UI controls on the Segments page. Slice 2 (PR #236, done): a churn feed — the most recently landed `stripe_subscription` entity records showing a churn signal (already canceled or scheduled to cancel), a new section on the billing-ops-feed page. Slice 3 (PR #238, done): generalized the shared per-schema-name query pattern (both KAN-80's billing feed and slice 2's churn feed used their own copy) into one exported `listRecentRecordsForSchemas`, and built a new schema-agnostic `/record-feed` admin page letting a human pick *any* registered event schema (not just the fixed billing/churn sets) and browse its recent records — rendered generically from the schema's own `field_defs`, with a PII-flagged field redacted server-side before the projection ever reaches the client. Slice 4 (PR #239, done): AI-suggested lists — a deterministic field-name/type heuristic (`packages/shared/src/segment-suggestion`, KAN-55's "stand-in for a real LLM call" posture) proposes new segment definitions from a project's registered entity schemas; a `SuggestSegmentsPanel` on the create-segment form seeds the form for the user to review and submit. (A third concurrent implementation, PR #242, was closed as superseded — see PROGRESS.md 2026-08-23.) Slice 5 (PR #244, done): CRM-sync action plugin — the outbound mirror of KAN-47's inbound source-plugin runtime (`SinkPluginExecutor`), a built-in generic authenticated-webhook connector (`plugin-runtime/crm-webhook`, reusing the existing `generic` `SharedCredentialModel` provider — no CRM-specific OAuth exists to build against yet), `listSegmentMembers` (a row-returning sibling of `countSegmentMembers`), and `syncSegmentToCrm` (retry/backoff, `PluginSinkRunModel` run history), wired to a "Sync to CRM" control on the Segments page. This closes out KAN-81 — all four Gap-5/9 pieces are now delivered. Slice 6 (PR #247, done): per-field filtering within the `/record-feed` page — an exact-match field/value filter scoped to the selected schema's non-PII fields only (so a filter value can never round-trip a PII value through the URL), `listRecentRecordsForSchemas` gained an optional `fieldFilter` (over-fetches a bounded 500-record candidate window before filtering, same posture as the churn feed's own signal filter). Found and fixed a real pre-existing bug while building it: `RawRecordModel.payload` is the whole ingest envelope (`event_id`/`event`/`ts`/`properties` for an event), not a flat field map, so `toRecordFeedEntryView` was reading every declared field from the wrong key — fixed by reusing `checkRecordEnvelope`. This closes Gap 5's "filterable" requirement — KAN-81 has no remaining scope. See PROGRESS.md for the same-bug-class follow-up flagged in `billing-ops-view.ts`/`isChurnSignal` (not fixed in PR #247 — different, already-merged features). |
| KAN-82 | E15.x Feedback & NPS: survey ingestion (connector + in-app SDK), NPS metrics + goal type, AI theme clustering into digest (`docs/plan/14-gap-analysis.md` Gap 1) | 2 | - | done | ~8d per the gap doc; split into a buildable-today slice (same posture KAN-18/KAN-27/KAN-81 established). Delivered: a `survey_response` event schema (`packages/shared/src/survey`, registered via `ensureSurveyResponseSchemaRegistered`) + an in-app survey SDK helper (`tracker.submitNpsSurvey(score, comment?)` in `packages/tracking-sdk`, a thin `track()` wrapper). A new dbt core mart `fact_survey_response` flattens the event's JSON `score`/`survey_type` into real columns (mirrors `fact_funnel_event`'s own `step` column — the metrics compiler only ever emits bare column references, no generic JSON extraction). A new built-in "Feedback & NPS" metric pack (`plugin-runtime/feedback-pack`, installed the same one-click way as the SaaS/Engagement/Landing-Page packs) registers `nps_respondents`/`nps_promoters`/`nps_detractors` (aggregation) + `nps_score` (formula: `(promoters - detractors) / respondents * 100`). Goal type: no code change needed — `GoalModel.metric_name` is already a free string validated against the active metric catalog, so `nps_score` works as a goal target as soon as the pack is installed. AI theme clustering: a deterministic keyword-taxonomy heuristic (`clusterFeedbackThemes`, same buildable-today stand-in posture KAN-55 established) clusters landed comments into a small fixed theme set (pricing/support/bugs/performance/missing_features/onboarding/usability) for a "top complaint this month" digest. Admin UI: a new project-scoped Feedback page (score + daily trend sparkline + theme digest), gated on `ingest.write` (same posture as billing-ops-feed/record-feed), showing the pack's one-click install card until installed. **Follow-up closed 2026-08-24 (PR #274):** NPS-by-plan/channel/cohort breakdown dimensions — `fact_survey_response` now left-joins the customer's current subscription plan (`dim_subscription`), last-touch marketing channel (`fact_attribution`), and signup cohort month (mirrors `fact_cancellation_reason`'s/KAN-84's exact join set); `nps_respondents`/`nps_promoters`/`nps_detractors` declare all three dimensions, and a new `getNpsDimensionBreakdownForProject` + `computeNpsScoreFromCounts` derive the per-dimension-value score (a formula metric like `nps_score` can't be broken down directly by the compiler); new breakdown tables on the Feedback page, degrading per-dimension when the warehouse isn't configured. Still deferred, same posture as Stripe/GA4/KAN-83/KAN-87: a real Delighted/Typeform third-party survey connector (needs a human-provisioned API key) — the in-app SDK path already covers ingestion. KAN-83's "onboarding-survey schema" is a distinct construct from this story's post-signup `survey_response` schema. |
| KAN-83 | E16.x Intent/quality scoring: onboarding-survey schema, at-signup quality score, quality-adjusted CAC/CPS + mix-shift alerts (`docs/plan/14-gap-analysis.md` Gap 4) | 2 | - | done | Delivered (PR #255): `onboarding_survey` event schema + `computeSignupQualityScore` (deterministic weighted heuristic, buildable-today stand-in for a real LLM/ML scoring call), `fact_signup_quality_score` dbt mart (joined to channel attribution + paying-customer flag), quality-adjusted CAC/CPS + a mix-shift "check now" alert (fire/still_active/resolve hysteresis), and a project-scoped Intent & Quality admin page gated on `ingest.write`. Deferred: a real onboarding-flow UI to collect the answers (in-app SDK path covers ingestion), and a richer paying-conversion join beyond the mart's boolean flag. |
| KAN-84 | E14.y Churn-reason capture: cancellation-reason schema (structured + free text), AI clustering into a live taxonomy, breakdown report joined to plan/channel/cohort (`docs/plan/14-gap-analysis.md` Gap 10) | 2 | - | done | Delivered (PR #252). Three sessions independently picked this story concurrently; #252 merged over PR #250 (closed as superseded — its CI never triggered in 3+ hours) and a third session's own implementation (discarded before push, PR #251) — see PROGRESS.md. |
| KAN-85 | E17.x Ergonomics: global omnisearch (cmd-K), inline target/value editing in tables, column sort/show-hide persistence (`docs/plan/14-gap-analysis.md` Gap 15) | 2 | - | done | ~6d per the gap doc; split into a buildable-today slice. Delivered (PR #253): global omnisearch — a Cmd/Ctrl-K palette (`packages/shared/src/omnisearch`'s pure ranking heuristic + `apps/web`'s per-project index over boards/metrics/segments/campaign targets, degrading per result type by permission rather than blanket-denying), wired into `AppShell`/`ProjectLayout`. Delivered (PR #269): column sort/show-hide persistence for board table tiles (`board-tile-view.tsx`'s `TableView`) — clickable, numeric-aware sortable headers and a per-column show/hide menu, both preferences persisted client-side in `localStorage` keyed by tile id (a display-only, per-viewer preference — `BoardTile`'s schema has no open config bag worth extending server-side for this). Delivered (PR #272): the final AC bullet, inline target/value editing in tables — the Goals list (`goals/page.tsx`) is now a table (name/metric/target/deadline/owner) with an inline-editable Target column (`GoalTargetInput`, a single numeric input for `maximize`/`minimize` goals or a rangeMin/rangeMax pair for `range` goals), committing on blur the same way KAN-86's `CampaignTargetInput` does. Goals previously had no update path at all (only create/delete) — added a direction-aware `updateGoal` to `goal.service.ts` (rejects `targetValue` on a `range` goal and vice versa, enforces `rangeMin < rangeMax`, before/after audit-logs as `goal.update`) plus a new `PATCH .../goals/[goalId]` route. This closes out KAN-85 — no remaining scope. |
| KAN-86 | E18.x Campaign ops: `roi_nd`/`collection_nd` metric family, per-campaign editable targets, predicted-vs-actual calibration views (`docs/plan/14-gap-analysis.md` Gap 12) | 2 | - | in-progress | ~7d per the gap doc; split into buildable-today slices. Delivered: a project-level `collection_Nd` fixed-window-payback metric family (7/14/30/40 days — `fact_customer_payback` dbt mart + a new "Campaign Ops" built-in metric pack) and per-campaign spend budget targets (`CampaignTargetModel`, inline-editable, red/green against `ad_spend`-by-`campaign_id` — the one metric this codebase carries that dimension on today). New project-scoped Campaign Ops admin page, gated on `dashboards.write`. Slice 2 (PR #271, done): predicted-vs-actual calibration views — a new `fact_quality_calibration` dbt mart joins each customer's predicted quality tier (KAN-83's `fact_signup_quality_score`, deduped to their latest-scored survey) to their actual revenue outcome (`fact_customer_payback`), bucketed low/medium/high; 5 new Campaign Ops pack metrics (`quality_calibration_signups`/`quality_calibration_paying_signups`/`quality_calibration_collected_revenue_40d` + two ratio formulas) and a new table on the Campaign Ops page (tier / signups / paying rate / avg 40-day revenue). Self-review before merge caught and fixed a real double-counting bug (event-grain join was replicating a customer's revenue total once per extra scored survey event) — see PROGRESS.md. This closes the AC's own "predicted-vs-actual calibration views" bullet. Still deferred, documented in the pack's own doc comment: `roi_nd` formulas (would need a cross-pack `ad_spend` dependency the install flow doesn't model yet — a human can already compose `collection_40d / ad_spend` by hand via the existing custom-metric evolve flow); true per-campaign `roi_nd`/`collection_nd` (blocked on both a real ad-spend connector, KAN-50/51/KAN-43, and a metrics-compiler join feature — `fact_revenue_event` carries no campaign_id). |
| KAN-87 | E19.x Firmographic enrichment: enrichment connector + AI industry classification, composition dashboards (# vs $), composition-shift alerts (`docs/plan/14-gap-analysis.md` Gap 11) | 3 | - | done | Delivered (PR #257): AI industry classification (`classifyCompanyIndustry`, deterministic keyword+TLD stand-in), `fact_company_firmographic` dbt mart joined to current subscription MRR, `firmographic_profiles_total`/`firmographic_mrr_total` metrics (# vs $ composition by industry/employee-count/region), and composition-shift alerts (`evaluateCompositionShift`, generalizing KAN-83's mix-shift rule to a bidirectional share delta). A real third-party enrichment connector (e.g. Clearbit) is deferred — needs a human-provisioned API key, same posture Stripe/GA4 established; the in-app SDK path (self-reported profile + client-side classification) covers ingestion for now. |
| KAN-88 | E20.x Rep-attributed collections ledger: activity ledger, rep-attributed collections, leaderboards (`docs/plan/14-gap-analysis.md` Gap 13) | 4 | - | done | Delivered (PR #264). The people-layer prerequisite this row previously flagged as missing turned out to already exist — `OrgPersonModel` (KAN-27) is exactly the `dim_team_member` registry Gap 6 calls for, already used as an owner reference by KAN-64 goals and KAN-81 segments. `RepCollectionEntryModel` (project-scoped ledger row: rep via `org_person_id`, nullable until attributed; company; type; plan from→to; amount; `occurred_at`; note; `source_raw_record_id` for billing-linked entries) + `rep-collection.service.ts` (create/update/delete/list, all audit-logged) delivers all three AC bullets: manual + auto attribution (`listBillingCollectionSignalsForProject` surfaces un-linked, successful, non-refunded Stripe charges as candidates a human confirms), weekly/monthly per-rep leaderboards (`getRepCollectionLeaderboardForProject`, ISO week or calendar month, unattributed collections kept as an explicit total rather than dropped), and war-room integration (a "Top collections this week" widget on the win-rules page). Admin UI gated on `dashboards.write`. **Follow-up delivered (2026-08-24):** the war-room widget's own doc comment had explicitly deferred the fuller `/tv` fullscreen-rotation integration as "a much larger, separately-scoped" piece of work — this run built it: the `/tv-pairing/rotation` manifest (`rotation/route.ts`) now also carries this week's `getRepCollectionLeaderboardForProject` result (reusing the same `toRepCollectionLeaderboardView` join the win-rules page already does), and `TvRotationScreen` rotates through a new leaderboard frame (reusing `RepCollectionLeaderboardWidget` as-is, no TV-only re-implementation) alongside the existing board and goals frames — omitted from the rotation entirely when there's nothing attributed or unattributed to show yet, mirroring the widget's own empty-state check. Two other sessions independently built KAN-88 concurrently (this repo's now-familiar collision pattern) — PR #263 (`CustomerOwnerModel`/`CollectionActivityModel` + a warehouse-derived metric pack) was closed as superseded after review: it explicitly deferred all three AC bullets PR #264 delivers (no time-windowed leaderboard, no billing auto-suggest, no war-room mention), though its self-review did catch a real, separate bug in the already-shipped SaaS pack — see KAN-59's row. A third local implementation (this run's own first attempt) was discarded before push once the collision was found. |
| KAN-89 | E-none Experimentation & A/B: exposure/conversion event schemas, `fact_experiment_event` mart, Experiment metric pack, results view with significance testing (`docs/plan/14-gap-analysis.md` Gap 3) | - | - | done | No KAN ticket existed for this gap-analysis bullet before this row (confirmed via Jira search) — added as KAN-89 per this repo's established "turn the next unaddressed gap bullet into a buildable-today slice" pattern (same as KAN-80/81/82/etc.). Delivered (PR #278, a parallel session's implementation, merged after this run's independent review): `experiment_exposure`/`experiment_conversion` event schemas (`packages/shared/src/experiments`, both carrying `experiment_key`/`variant_key` — the client library that bucketed a visitor into a variant is expected to stamp it on the later conversion call too, avoiding a touchpoint/attribution-style join); a new dbt core mart `fact_experiment_event` flattening both event types into real columns; a built-in "Experiments" metric pack registering `experiment_exposures`/`experiment_conversions` (distinct-customer counts broken down by `experiment_key`/`variant_key`); `computeExperimentResult` (`packages/shared`) — a dependency-free two-proportion z-test (Abramowitz & Stegun erf approximation) picking a control variant (literal `"control"` key, else alphabetically first) and computing each other variant's conversion rate, uplift, p-value, and significance (95% confidence) against it; a new project-scoped Experiments admin page (one results table per experiment, degrading cleanly when the warehouse isn't configured), gated on `ingest.write`, same posture as Feedback/Churn Reasons/Firmographics. **Caught before merge:** the PR branch's own "merge main" commit was stale (two commits behind `origin/main`, missing KAN-88's `/tv` rotation leaderboard, PR #276/#277) and would have silently reverted that feature on squash-merge despite GitHub reporting it cleanly mergeable — re-merged current `main` in, verified the three affected files came back byte-identical to `main`, and re-verified the full monorepo test suite before pushing the fix. Deferred, same posture KAN-82/KAN-87 establish for their own gap-analysis stories: a real external experimentation-tool integration (GrowthBook/Optimizely/VWO) and an AI-suggested rollout verdict. |
| KAN-90 | E-none Customer-support analytics & team leaderboards: support-ticket event schema, `fact_support_ticket_event` mart, Customer Support metric pack, per-agent leaderboard, good_review win type (`docs/plan/14-gap-analysis.md` Gap 6) | - | - | done | No KAN ticket existed for this gap-analysis bullet before this row — the only one left unaddressed after KAN-80..89 — added as KAN-90 per this repo's established "turn the next unaddressed gap bullet into a buildable-today slice" pattern. Delivered (PR #282): `support_ticket_event` event schema (`packages/shared/src/support`, one schema/two `stage` values `opened`/`resolved`, mirroring `SURVEY_RESPONSE_SCHEMA_FIELDS`'s stage-specific-optional-fields shape — a `resolved`-stage event carries the connector-computed `first_response_seconds`/`resolution_seconds`/`csat_score` deltas directly, no dbt self-join needed, same tradeoff `experiment_key`/`variant_key` accept); a bare-flatten `fact_support_ticket_event` dbt mart (no joins, same shape `fact_experiment_event` establishes); a built-in "Customer Support" metric pack (`plugin-runtime/support-pack`) registering `support_tickets_opened`/`support_tickets_resolved`/`support_avg_first_response_seconds`/`support_avg_resolution_seconds`/`support_avg_csat_score` (broken down by `agent_org_person_id`) plus a `support_open_backlog` formula; a live per-agent leaderboard (`getSupportLeaderboardForProject`, reads landed raw records directly and aggregates in TypeScript — no warehouse dependency, same posture `getNpsOverviewForProject`/`aggregateRepCollectionLeaderboard` establish) resolved against `OrgPersonModel` for name/photo; a new project-scoped Support admin page gated on `ingest.write`; and a `good_review` win-catalog entry (`WIN_TYPES`) + war-room chime so a CSAT-crossing-threshold ticket resolution can fire a war-room celebration. Self-review (`/code-review`) before merge found one real, documented limitation (not fixed, since the metrics-compiler formula parser has no clamp/greatest function): `support_open_backlog` has no floor at zero on a board tile/goal, unlike the leaderboard service's own `Math.max(0, ...)` clamp for the identical computation — noted in the metric's own doc comment. Deferred, same posture KAN-82/KAN-84/KAN-87/KAN-89 establish for their own gap-analysis stories: a real Zendesk/Intercom/Freshdesk/Crisp connector (needs a human-provisioned API key). Follow-up closed 2026-08-25 (KAN-91, PR #285): the clamp/greatest limitation itself. |
| KAN-91 | E-none Metrics-compiler `max()`/`min()` formula functions, so a formula metric can express a floor/ceiling | - | - | done | No KAN ticket existed for this — a self-contained, no-infra-required follow-up found doing the "sweep every `done` row's own deferred/not-yet notes for a newly-buildable follow-up" pass PROGRESS.md's own prior entries recommend once every `docs/plan/14-gap-analysis.md` bullet had a story (KAN-80..90). KAN-90's own doc comment on `support_open_backlog` names the exact gap: the formula parser (`packages/shared/src/metrics-compiler/formula-parser.ts`) only supported `+`/`-`/`*`/`/`, so a formula metric could never floor/ceiling itself the way `SupportLeaderboardResult.openBacklog`'s own `Math.max(0, ...)` does for the identical computation. Delivered: a `call` AST node (`max`/`min`, a closed function-name set — an unrecognized name throws immediately as a likely typo rather than being silently accepted) with comma-separated argument parsing, compiling to BigQuery's variadic `GREATEST`/`LEAST` (`compiler.ts`); `collectIdentifiers` recurses into a call's arguments without ever treating the function name itself as a referenced-metric identifier (the correctness-critical part — `metric-registry.service.ts`'s existence/cycle validation resolves every formula reference against the active-metric catalog, so `max`/`min` must never be mistaken for a metric name needing to exist). `metric-registry.service.ts`'s own hand-rolled formula validation (a `FORMULA_ALLOWED_CHARS_PATTERN` character-class regex + a separate `hasBalancedParens` walk + a separate identifier-extraction regex) is deleted in favor of reusing the shared parser directly (`parseFormula`/`collectIdentifiers`, the same functions `metrics-compiler.service.ts` already uses to resolve transitive formula references) — both a reuse win and the only way to get `max`/`min` support there without a second, parallel function-name carve-out. `support_open_backlog` updated to `max(support_tickets_opened - support_tickets_resolved, 0)`, closing the KAN-90 limitation its own doc comment named. New coverage: `formula-parser.test.ts` (call parsing, precedence composition, unknown-function/too-few-args errors, `collectIdentifiers` never surfacing the function name), a new golden-file compiler fixture (case 14, `max()` → `GREATEST()`) + a direct `min()`/`LEAST()` + 3-argument-call unit test, and `metric-registry.emulator.test.ts` (registers a `max()`/`min()` formula against a real Firestore emulator; rejects an unsupported function name and a too-few-argument call). The metric-defs admin form's formula placeholder (`en`/`he`) now shows a `max(...)` example. |
| KAN-92 | E-none Sales-assist workflows (demos pipeline): demo/meeting event schema, `fact_demo_event` mart, Sales Pipeline metric pack (scheduled/held/no-show/show rate), per-rep breakdown, recent-demos feed (`docs/plan/14-gap-analysis.md` Gap 9) | - | - | done | Delivered (PR #287). No KAN ticket existed for this gap-analysis bullet before this row — the last of the 15 `docs/plan/14-gap-analysis.md` gap bullets left unaddressed after KAN-80..91 — added as KAN-92 per this repo's established "turn the next unaddressed gap bullet into a buildable-today slice" pattern. Delivered: a `demo_event` event schema (`packages/shared/src/sales`, one schema/four `stage` values `scheduled`/`held`/`no_show`/`canceled`, mirroring `SUPPORT_TICKET_SCHEMA_FIELDS`'s (KAN-90) stage-specific-optional-fields shape); a bare-flatten `fact_demo_event` dbt mart (no joins, same shape `fact_support_ticket_event` establishes); a built-in "Sales Pipeline" metric pack (`plugin-runtime/sales-pack`) registering `demos_scheduled`/`demos_held`/`demos_no_show` (broken down by `rep_org_person_id`) plus a `demo_show_rate` formula; a live demo funnel + per-rep breakdown (`getDemoFunnelForProject`, reads landed raw records directly and aggregates in TypeScript — no warehouse dependency, same posture `getSupportLeaderboardForProject` establishes) resolved against `OrgPersonModel` for name/photo; a new project-scoped Demos admin page gated on `ingest.write`. The AC's "recent demos feed" is satisfied by linking to the existing generic `/record-feed` page (KAN-81) rather than duplicating it — once the pack registers the `demo_event` schema, it's automatically browsable there. The AC's "paying_no_demo-style work list via Gap 5's segments" is deliberately not built: KAN-76/81's segment engine (`segment.service.ts`) only ever filters one *entity* schema's own fields, with no cross-schema join or negation, so "paying AND no demo" isn't expressible today without either a denormalized demo-status field on a paying-customer entity (no connector populates one) or a segment-engine cross-schema join feature — both out of scope, documented on the Demos page's own doc comment; a human can already build that exact list with the existing Segments page once such a field exists. A real calendar/CRM connector (Calendly, HubSpot, Salesforce) is deferred — needs a human-provisioned API key, same posture Stripe/GA4/KAN-82/KAN-84/KAN-87/KAN-90 established for their own third-party connectors. This closes out every `docs/plan/14-gap-analysis.md` gap bullet (1-15). |
